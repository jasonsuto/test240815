# Used by delivery/jitpack/install-to-m2.sh
#
# groupId (multi-module Android on JitPack):
#   Must be com.github.<GitHubUser>.<GitHubRepoName>
#   Example repo github.com/jasonsuto/250924-mapsgl-android-sdk →
#   groupId=com.github.jasonsuto.250924-mapsgl-android-sdk
#   (If your repo slug differs, change this to match JitPack “Get it”.)
#
# artifactId: Gradle module name (e.g. mapsglmaps). Consumers use:
#   implementation 'com.github.USER.REPO:mapsglmaps:Tag'
#
# version: Used for LOCAL mvn tests only. On JitPack (env JITPACK=true), the
#   script overrides this with the git ref JitPack is building (see install-to-m2.sh).

groupId=com.github.jasonsuto.250924-mapsgl-android-sdk
artifactId=mapsglmaps
version=v1.5.0
